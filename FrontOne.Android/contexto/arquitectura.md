# Arquitectura, decisiones y estado — FrontOne.Android

> Parte de la memoria viva del sub-proyecto — ver [contexto.md](../contexto.md) para el índice. Reglas duras viven en [CLAUDE.md](../CLAUDE.md).

## Decisiones de stack (2026-08-07, sesión de arranque)

Tras una ronda de lluvia de ideas comparando Java/Kotlin/Kotlin Multiplatform/Flutter y MVVM-simple/Clean-Architecture/Hexagonal, se confirmó:

- **Lenguaje/UI**: Kotlin + Jetpack Compose. Se descartó Flutter por incompatibilidad directa con la regla dura de conexión a SQL Server (no existe driver JDBC/ADO maduro para Dart, forzaría una API intermedia). Se descartó Kotlin Multiplatform por sobre-ingeniería — no hay hoy un segundo cliente nativo (iOS) planeado que justifique compartir código; si aparece en el futuro, la capa `:domain`/`:data` en Kotlin puro ya es compatible con migrar a KMP sin reescribir.
- **Arquitectura**: Hexagonal ligera (puertos/adaptadores), espejo de las capas que ya tiene FrontOne en C# (`Domain`/`Application`/`Infrastructure.SqlServer`/`WinForms`). Se descartó DDD táctico completo (Value Objects, Aggregate Roots, CQRS) por ser desproporcionado para una app que arranca como extensión ágil.
- **Datos**: conexión directa a SQL Server vía `mssql-jdbc` (driver oficial de Microsoft), mismo servidor que usa FrontOne de escritorio, todo por Stored Procedures. Se descartó jTDS (proyecto abandonado) y R2DBC (soporte de SQL Server inmaduro).
- **Alcance**: catálogos/permisos/roles/módulos ya construidos se quedan exclusivamente en escritorio. Android es una extensión para funcionalidad nueva que eventualmente migra a WinForms — la lógica de negocio real debe vivir donde sea fácil de portar (capa `usecase`/SPs), no enterrada en el ViewModel.
- **Red**: dispositivos siempre en red local/VPN de la empresa (confirmado con el usuario) — por eso se decidió *online-only*, sin caché/sincronización offline por ahora.
- **Ubicación**: `FrontOne.Android/` como carpeta hermana de `FrontOne.WinForms/` dentro de `E:\FrontOne` (un solo repo, contexto compartido), no un repositorio separado.

## Estado actual: scaffold inicial creado

Estructura Gradle multi-módulo (3 módulos = las 3 capas hexagonales, ver tabla en `CLAUDE.md`):

```
FrontOne.Android/
  settings.gradle.kts, build.gradle.kts, gradle.properties
  gradle/wrapper/gradle-wrapper.properties   (jar del wrapper NO generado, ver nota abajo)
  domain/   → Kotlin puro. model/ResultadoConexion.kt, port/ConexionSqlServerPort.kt, usecase/ProbarConexionUseCase.kt
  data/     → Kotlin + mssql-jdbc. sqlserver/{ConfiguracionConexion,ConnectionFactory,SqlRepositoryBase,ConexionSqlServerAdapter}.kt
  app/      → Android + Compose + Hilt. FrontOneApplication.kt, MainActivity.kt, di/DataModule.kt, ui/theme/Theme.kt, ui/conexion/{Screen,ViewModel}.kt
```

**Caso de uso piloto**: `ProbarConexionUseCase` — un flujo end-to-end completo (puerto → adaptador JDBC → Hilt → ViewModel → Compose) que ejecuta `SELECT @@VERSION` contra SQL Server y muestra el resultado en pantalla. No es un módulo de negocio real, es la plantilla de referencia para verificar que las 3 capas están correctamente conectadas antes de construir el primer módulo real. Excepción documentada a la regla "todo por SP": esta única consulta no es dato de negocio, no amerita un Stored Procedure — ver comentario en `ConexionSqlServerAdapter.kt`.

**Pendiente, no resuelto todavía en el scaffold**:
- El wrapper de Gradle (`gradlew`/`gradlew.bat` + `gradle-wrapper.jar`) no se generó — el jar es un binario que no se puede crear por texto. Al abrir la carpeta en Android Studio, este genera/gestiona el wrapper automáticamente; si se prefiere la terminal, correr `gradle wrapper` una vez con una instalación local de Gradle.
- `di/DataModule.kt` tiene credenciales de conexión **hardcodeadas como placeholder** (`servidor = "172.16.1.100"`, login/password `"TODO_..."`) — deliberado, solo para que el scaffold compile. Antes de probar contra un servidor real hay que reemplazarlas a mano (o construir ya la pantalla de configuración con `EncryptedSharedPreferences`, ver pendiente de `CLAUDE.md`).
- No hay login SQL dedicado definido todavía — el placeholder asume que se creará antes de la primera prueba real contra `172.16.1.100\FrontOne` o el servidor que corresponda al entorno.
- Ningún módulo de negocio real construido todavía — el primer módulo se elige en una sesión futura (se evaluaron como candidatos: Recepción de Fruta, escaneo GS1-128, dashboard de Almacenes de solo lectura, ver la conversación de brainstorming original).

## Cómo compilar (una vez resuelto el wrapper)

```
./gradlew :app:assembleDebug
```

o abrir la carpeta `FrontOne.Android/` directo en Android Studio (detecta el `settings.gradle.kts` y sincroniza los 3 módulos automáticamente).

## Iteración: ajuste de versiones para sincronizar en la máquina real del usuario

Primera sincronización real en Android Studio (`E:\android`, versión AI-261 / "2026.1.3") destapó una cadena de incompatibilidades de versión — herramientas de punta (JDK 25/26 recién salidos) contra un scaffold armado con versiones "seguras" de 2024. Resuelto en cascada:

1. **Gradle 8.9 no soporta JDK > ~23** ("Incompatible Gradle JVM version") → se subió el wrapper a **Gradle 9.0.0** (`gradle/wrapper/gradle-wrapper.properties`), misma versión que ya estaba instalada localmente en `C:\gradle\gradle-9.0.0`.
2. **Gradle 9.0.0 tampoco soporta JDK 25/26** — tope real de esa versión es **JDK 24**. Se seleccionó JDK 21 (`jbr-21`, JetBrains Runtime bundleado con Android Studio) como Gradle JDK del proyecto — no es una limitación del proyecto, es un techo real de Gradle 9.0.0 documentado por el propio IDE.
3. **Kotlin 2.0.21 no soporta JDK > 25** ("Incompatible JDK Version", pide Kotlin ≥ 2.1.10) → se subió el plugin de Kotlin a **2.1.10** en `build.gradle.kts` raíz (`org.jetbrains.kotlin.android`/`.jvm`), y KSP a `2.1.10-1.0.29` (debe ir siempre pegado a la versión de Kotlin, formato `{kotlinVersion}-{kspVersion}`).
4. Se subió **AGP de 8.6.0 a 8.9.1** en el mismo movimiento, para no quedar con un Gradle 9.0 corriendo un AGP viejo (riesgo de un cuarto error de compatibilidad encadenado).
5. **Efecto colateral necesario, no relacionado a las versiones de JDK**: Kotlin 2.0+ requiere aplicar el plugin `org.jetbrains.kotlin.plugin.compose` por separado en cualquier módulo con `buildFeatures.compose = true` — antes del Kotlin Gradle Plugin 2.0 el compilador de Compose se configuraba con `composeOptions.kotlinCompilerExtensionVersion`, eso ya no aplica. Se agregó el plugin en `build.gradle.kts` raíz y en `app/build.gradle.kts`; el scaffold original (creado antes de esta sincronización real) no lo tenía y hubiera fallado en la primera compilación de una pantalla Compose aunque el resto sincronizara bien.

**Lección para sesiones futuras**: en máquinas con herramientas muy recientes (JDK 25/26, Android Studio de builds nuevos), sincronizar una sola vez en el IDE real antes de dar por bueno un scaffold armado a ciegas — las versiones "estables conocidas" al momento de escribir el código quedan obsoletas rápido frente al entorno real del usuario. Ninguno de estos 4 ajustes cambió una sola línea de la arquitectura/código de negocio (`domain`/`data`/`ui`), solo números de versión de plugins — la separación de capas hexagonales absorbió el problema sin tocar lógica.

**Confirmado por el usuario (2026-08-07)**: sincronización completa en Android Studio real (`E:\android`) sin errores, con Gradle JDK 21 (`jbr-21`). El scaffold de las 3 capas hexagonales (`:domain`/`:data`/`:app`) queda validado end-to-end contra herramientas reales, no solo en teoría.

## Iteración: mssql-jdbc no funciona en runtime Android — se cambió a jTDS

Primera corrida real en dispositivo/emulador contra `172.16.1.100\FrontOne` (usuario `sa`, solo para esta prueba de humo — sigue pendiente el login dedicado). Crash inmediato al presionar "Probar Conexión":

```
FATAL EXCEPTION: main
java.lang.AssertionError: numMsgsRcvd:1 should be less than numMsgsSent:1
    at com.microsoft.sqlserver.jdbc.TDSReader.readPacket(IOBuffer.java:6879)
    ...
    at com.android.org.conscrypt.ConscryptEngineSocket.doHandshake(ConscryptEngineSocket.java:242)
    at com.microsoft.sqlserver.jdbc.TDSChannel.enableSSL(IOBuffer.java:1854)
    at com.microsoft.sqlserver.jdbc.SQLServerConnection.connectHelper(SQLServerConnection.java:3797)
```

**Causa raíz**: `mssql-jdbc` implementa su fase de "login-only encryption" (el handshake TLS obligatorio que envuelve el paquete `LOGIN7` incluso cuando `encrypt=false`) con un wrapper de socket propio que asume el comportamiento de un `SSLSocket` de un JSSE de escritorio estándar. `Conscrypt` (el proveedor SSL por defecto de Android) maneja el buffering de registros TLS de forma distinta, y esa asunción interna del driver revienta con un `AssertionError` — es una incompatibilidad conocida y documentada de `mssql-jdbc` sobre Android, no un error de configuración del proyecto. Confirma lo que ya se había marcado como riesgo desde que se eligió el driver (ver sección "Alternativas de conectividad" de la conversación original): Microsoft no certifica `mssql-jdbc` para Android.

**Fix**: se cambió el driver de `:data` de `mssql-jdbc` a **jTDS** (`net.sourceforge.jtds:jtds:1.3.1`) — no implementa ese mecanismo propietario de TLS-sobre-TDS, así que no pisa el bug. Cambios:
- `data/build.gradle.kts`: dependencia reemplazada.
- `ConfiguracionConexion.kt`: cadena JDBC cambió de formato `jdbc:sqlserver://...` a `jdbc:jtds:sqlserver://...`; campos `cifrarConexion`/`confiarEnCertificadoServidor` (semántica de mssql-jdbc) reemplazados por `usarSsl: Boolean = false` (semántica de jTDS, `ssl=off|require`). Default `false` porque ya se había acordado que la red es local/VPN confiable — evita reintroducir el mismo tipo de problema hasta que haya una razón real para cifrar en tránsito.
- `ConnectionFactory.kt`: se agregó `Class.forName("net.sourceforge.jtds.jdbc.Driver")` explícito en el `init` — no confiar en el auto-registro por `ServiceLoader`/`META-INF/services` de `DriverManager`, que en Android no siempre es confiable.
- `app/proguard-rules.pro`: nota actualizada al driver nuevo.

**No cambió nada de la arquitectura ni de `SqlRepositoryBase`** — el `ConexionSqlServerPort`/`ConexionSqlServerAdapter` siguen exactamente igual, solo cambió qué hay detrás de `ConnectionFactory.abrirConexion()`. Es la prueba de que separar en capas hexagonales valió la pena: un problema de infraestructura real (driver incompatible con la plataforma) se resolvió tocando solo `:data`, sin tocar `:domain` ni `:app`.

**Confirmado por el usuario (2026-08-07), en hardware real**: corrida en un terminal Honeywell EDA52 (dispositivo físico, no emulador — coincide con el perfil de hardware esperado para el caso de uso real de campo/planta) contra `172.16.1.100\FrontOne`. Pantalla mostró "Conexión exitosa. Servidor: Microsoft SQL Server 2022 (RTM-GDR) (KB5102334) - 16.0.1190.2 (X64)".

**Con esto queda cerrado el objetivo del scaffold**: las 3 capas hexagonales (`:domain`/`:data`/`:app`), Compose, Hilt, y la conectividad real a SQL Server (con jTDS) están verificadas de punta a punta en hardware real, no solo en teoría ni en compilación.

## Iteración: pantalla de Login (visual) + primer caso de uso de negocio real (logo) + Configuración de Conexión

Pedido del usuario: diseño profesional para la pantalla de login, integrar el logo real de la empresa, y mover el "modo desarrollador" a un ícono de ajustes que abra una pantalla de configuración de conexión (como `ConfiguracionConexionesForm` de escritorio). Se buscó primero un archivo de logo en todo el repo (`find` por extensión de imagen + `grep` de "fronterra") — no existe ninguno; el logo de la empresa vive únicamente como dato binario en `Configuracion.Empresa.Logo` (`VARBINARY(MAX)`), cargado por el usuario desde `ConfiguracionEmpresaForm` en escritorio. El usuario pidió traerlo de la base de datos en vivo, no como asset estático — esto lo convirtió en el **primer caso de uso de negocio real del proyecto** (todo lo anterior era el piloto de conectividad).

### Caso de uso: logo de Configuracion.Empresa

Patrón hexagonal completo de 6 piezas (mismo que documenta `CLAUDE.md`):
- `domain/port/EmpresaPort.kt` — `suspend fun obtenerLogo(): ByteArray?`. El dominio pide bytes crudos, nunca conoce `android.graphics.Bitmap` (eso es UI, vive en `:app`).
- `domain/usecase/ObtenerLogoEmpresaUseCase.kt`.
- `data/sqlserver/EmpresaSqlServerAdapter.kt` — extiende `SqlRepositoryBase` (primer uso real de esa clase base, antes solo la implementaba el piloto de conexión sin heredar de ella) y llama `Configuracion.sp_Empresa_Obtener` (el mismo SP que ya usa FrontOne de escritorio, sin parámetros — tabla singleton Id=1), leyendo la columna `Logo` con `ResultSet.getBytes("Logo")`.
- Binding en `app/di/DataModule.kt`.
- `app/ui/login/LoginViewModel.kt` — decodifica los bytes a `Bitmap`/`ImageBitmap` (única capa que puede, por tener acceso a `android.graphics`), con `sealed interface EstadoLogo` (Cargando/Disponible/NoDisponible). Si la consulta falla (sin red, tabla vacía, lo que sea) degrada a `NoDisponible` en silencio — el login debe seguir siendo usable aunque el logo no cargue, no es una función crítica.
- `LoginScreen.kt` muestra un ícono `Business` genérico como placeholder mientras no hay logo real cargado en la base.

### Rediseño visual del login

Paleta propia en `ui/theme/Theme.kt` (antes `lightColorScheme()`/`darkColorScheme()` vacíos, heredaban el morado default de Material3 sin intención) — azul/índigo marcado como **placeholder explícito**, no decisión de marca final, hasta que exista una paleta oficial de Fronterra. `LoginScreen.kt` reestructurado: `Surface` de fondo + `Card` con esquinas redondeadas conteniendo los campos + botón de altura fija, tipografía con jerarquía (headline para "FrontOne", body para el subtítulo).

### Configuración de Conexión — reemplaza el "modo desarrollador" + implementa `EncryptedSharedPreferences`

Esto resolvió de una vez el pendiente que había quedado abierto desde el arranque del proyecto ("mover credenciales de `BuildConfig` a `EncryptedSharedPreferences`"):

- **`app/config/ConfiguracionConexionStore.kt`** (nuevo, vive en `:app` — no en `:data`/`:domain`, porque `EncryptedSharedPreferences` necesita `Context` de Android, y la regla hexagonal del proyecto es que ni el dominio ni el adaptador de datos pueden depender del framework de Android). Lee/guarda servidor/puerto/base de datos/usuario/password cifrados (respaldados por Android Keystore, `MasterKey.KeyScheme.AES256_GCM`). Si el usuario nunca guardó nada (primer arranque), cae de vuelta a los valores de `BuildConfig`/`secrets.properties` — así el flujo de desarrollo original sigue funcionando sin configurar nada a mano.
- **`ConnectionFactory` cambió de recibir un `ConfiguracionConexion` fijo a recibir un `() -> ConfiguracionConexion`** (`data/sqlserver/ConnectionFactory.kt`) — se consulta en cada `abrirConexion()`, no una sola vez al armar el grafo de Hilt. Así, si el usuario guarda credenciales nuevas desde la pantalla de Configuración, la siguiente operación las usa de inmediato, sin reiniciar la app. `:data` sigue sin conocer `EncryptedSharedPreferences` ni Android — solo recibe una función, mantiene la pureza de la capa.
- **`ui/configuracion/ConfiguracionConexionScreen.kt` + `ConfiguracionConexionViewModel.kt`** (nuevo, equivalente móvil de `ConfiguracionConexionesForm.cs`): campos Servidor/Puerto/Base de datos/Usuario/Contraseña, botones "Guardar" y "Probar Conexión". "Probar Conexión" primero guarda los valores capturados y luego corre `ProbarConexionUseCase` — como `ConnectionFactory` ahora lee del store en cada llamada, esto garantiza que la prueba use exactamente lo que el usuario acaba de escribir, no un valor viejo en memoria.
- **`MainActivity.kt`**: el botón de texto "Modo desarrollador: Probar Conexión" se reemplazó por un `FloatingActionButton` con ícono de ajustes (`Icons.Filled.Settings`), abajo a la derecha — abre `ConfiguracionConexionScreen`. La pantalla `ui/conexion/ProbarConexionScreen.kt` (el piloto original) se queda en el proyecto como plantilla de referencia documentada en `CLAUDE.md`, pero ya no está enganchada a ningún flujo de navegación — su funcionalidad quedó absorbida por la pantalla de Configuración.
- Dependencia nueva: `androidx.security:security-crypto:1.1.0-alpha06`. También se cambió `material-icons-core` (agregado unos minutos antes, para el ícono `Business` del logo) por **`material-icons-extended`** — `Business`/`Settings` no están en el subconjunto chico de `-core`.

**Pendiente, no resuelto en esta iteración**:
- Login SQL dedicado (todo sigue probándose con `sa`) — sigue igual de pendiente que antes, ahora es más fácil de rotar porque ya existe la pantalla de Configuración para cambiar el usuario sin tocar código.
- La pantalla de Login sigue sin funcionalidad real de autenticación (el botón "Iniciar Sesión" no llama nada todavía) — el usuario pidió explícitamente construir primero solo la parte visual.
- Verificación en vivo de esta iteración completa (logo real cargando, pantalla de Configuración guardando/probando) — pendiente de que el usuario compile y pruebe en el Honeywell EDA52.

### Fix de compilación: import de `weight` sobrando en ConfiguracionConexionScreen.kt

Al compilar salió `Cannot access 'val RowColumnParentData?.weight: Float': it is internal in file`, apuntando al import `androidx.compose.foundation.layout.weight`. Primer intento (equivocado): se asumió desfase de versión Compose-Kotlin y se subió `compose-bom` de `2024.12.01` a `2026.06.01` (verificado contra el catálogo oficial de BOMs de Google, que efectivamente ya llega hasta esa versión — cambio válido igual, el proyecto estaba usando un BOM más de un año atrasado frente al resto del stack). El error persistió idéntico después de eso, lo que descartó la hipótesis de versión.

**Causa real**: `weight` no es una función de nivel superior del paquete `androidx.compose.foundation.layout` — es un miembro extensión de las interfaces `RowScope`/`ColumnScope`, ya visible automáticamente dentro del lambda de contenido de un `Row {}`/`Column {}` sin necesidad de ningún `import`. El import explícito enganchaba un símbolo distinto (de visibilidad interna, pensado para uso interno de la librería), no la función pública real. Se quitó el import — `Modifier.weight(1f)` dentro del `Row` de los botones Guardar/Probar Conexión sigue funcionando igual, solo que resuelto por scope implícito.

**Lección para sesiones futuras**: si el autocompletado del IDE ofrece importar `weight` (o cualquier extensión de `RowScope`/`ColumnScope`/`BoxScope`) como símbolo de paquete, no aceptarlo — esas funciones nunca se importan, ya están disponibles dentro del scope correspondiente.

## Iteración: autenticación real del login contra Seguridad.Usuario

Se investigó primero (agente de exploración) cómo funciona el login real en FrontOne de escritorio antes de portar nada, para no inventar un esquema de autenticación paralelo. Hallazgos clave de `LoginForm.cs`/`AuthService.cs`/`CryptoService.cs`/`sp_Usuario_ObtenerPorNombreUsuario` (C#):

- El SP **no compara nada** — solo trae la fila completa de `Seguridad.Usuario` (incluido `PasswordEncriptado`) por nombre de usuario.
- La comparación pasa en la capa Application: se descifra `PasswordEncriptado` (AES-256-CBC, clave e IV derivados por PBKDF2-HMAC-SHA256 de una passphrase/salt **fijas y hardcodeadas en el código fuente**, 100,000 iteraciones) y se compara el texto plano contra lo que escribió el usuario — no es un hash de una vía, es cifrado simétrico reversible.
- Mensaje de error siempre genérico ("Usuario o contraseña incorrectos"), sin distinguir usuario inexistente / inactivo / password incorrecta.
- Sin JWT/token — es una validación puntual; `SessionContext` (WinForms) guarda el usuario + permisos en memoria para el resto de la sesión de escritorio.

**Decisión de seguridad discutida explícitamente con el usuario antes de escribir código**: portar esta misma lógica a Android implica empotrar la passphrase/salt AES en el APK, para poder descifrar `PasswordEncriptado` en el dispositivo — cualquiera que decompile el APK obtiene la clave maestra que descifra las contraseñas de **todos** los usuarios de la base, no solo la del dispositivo. Se le presentaron dos opciones (portar igual que escritorio vs. mover la validación a un SP nuevo del lado servidor que nunca exponga la clave) y **se confirmó portar igual que escritorio**, con el razonamiento explícito de que esto no introduce una categoría de riesgo nueva — la misma clave ya vive hoy hardcodeada en `FrontOne.WinForms.exe`, y decompilar un ensamblado .NET (ILSpy/dnSpy, gratis) es igual de trivial que decompilar un APK. Es una decisión de diseño que el proyecto ya tenía asumida desde escritorio; Android simplemente la iguala, no la empeora. Si en el futuro se quiere cerrar esta brecha de verdad, habría que resolverlo para **ambas** plataformas a la vez (mover la validación a un SP/servicio que nunca entregue la clave a ningún cliente), no como un parche solo de Android.

### Implementación (patrón hexagonal completo, tercer caso de uso de negocio real del proyecto)

- **`domain/crypto/CryptoService.kt`** (nuevo paquete `domain/crypto/`, no es un puerto/adaptador — es una réplica bit a bit del algoritmo de C#, sin necesidad de ser mockeable/intercambiable): mismas constantes exactas (`PASSPHRASE`, `SALT`, `KEY_SIZE_BITS=256`, `ITERATIONS=100_000`), derivación vía `SecretKeyFactory.getInstance("PBKDF2WithHmacSHA256")` + `PBEKeySpec` (384 bits derivados = 32 bytes clave + 16 bytes IV), descifrado vía `Cipher.getInstance("AES/CBC/PKCS5Padding")` — `PKCS5Padding` de Java es el mismo esquema que `PKCS7Padding` de .NET para bloques de 16 bytes, no hay discrepancia real pese al nombre distinto. Solo implementa `decrypt` (Android nunca necesita cifrar contraseñas nuevas, esa función se queda exclusiva de escritorio). Vive en `:domain` porque `javax.crypto` es JDK estándar, no requiere Android — no rompe la regla hexagonal de mantener el dominio libre de dependencias de plataforma.
- **`domain/model/Usuario.kt`**: `Usuario` (público, sin password — equivalente a `UsuarioDto.cs`) y `UsuarioAutenticacion` (interno al caso de uso, con `passwordEncriptado`, nunca sale de `LoginUseCase`).
- **`domain/model/ResultadoLogin.kt`**: `sealed interface` `Exitoso(usuario)`/`Fallido(mensaje)`.
- **`domain/port/UsuarioPort.kt`** + **`domain/usecase/LoginUseCase.kt`**: réplica exacta del flujo de `AuthService.LoginAsync` — trae usuario, rechaza si no existe/inactivo, descifra y compara, mensaje de error genérico único en los 3 casos de rechazo. Solo el nombre de usuario se recorta (`trim()`), el password no — igual que C#.
- **`data/sqlserver/UsuarioSqlServerAdapter.kt`**: extiende `SqlRepositoryBase`, llama `Seguridad.sp_Usuario_ObtenerPorNombreUsuario` (el mismo SP que ya usa escritorio, sin cambios) con `@NombreUsuario` como único parámetro.
- Binding en `DataModule.kt`.
- **`LoginViewModel.kt`** ganó `estadoLogin: StateFlow<EstadoLogin>` (`Inicial`/`Autenticando`/`Exitoso`/`Error`) y `fun iniciarSesion(usuario, contrasena)` — valida que no vengan vacíos antes de llamar al caso de uso (guarda de UI, no reemplaza la validación real del `LoginUseCase`).
- **`LoginScreen.kt`**: botón "Iniciar Sesión" ahora llama `viewModel.iniciarSesion(...)` de verdad; muestra spinner dentro del propio botón mientras autentica (campos deshabilitados en ese momento), mensaje de error en rojo debajo de los campos, y un mensaje de bienvenida con `NombreCompleto` si el login es exitoso. Ganó un parámetro `onLoginExitoso: (Usuario) -> Unit = {}` sin uso real todavía — no hay pantalla "de inicio" a la cual navegar tras el login, se deja listo para cuando exista el primer módulo de negocio real, así no hay que volver a tocar esta pantalla en ese momento.

**Pendiente, no resuelto en esta iteración**:
- No hay navegación post-login — `onLoginExitoso` no hace nada todavía, el mensaje de bienvenida es el único feedback visible de un login correcto.
- Login SQL dedicado sigue pendiente (todo sigue usando `sa`).

**Confirmado por el usuario (2026-08-07)**: probó con usuario/contraseña reales de `Seguridad.Usuario` — login exitoso, entra bien. Login real funcionando de punta a punta contra la BD real.

## Iteración: splash screen con color de fondo extraído del logo

El usuario pidió una pantalla de bienvenida a pantalla completa mostrando el logo en grande, con un fondo elegido para que el logo "no se pierda" (mandó como referencia visual el splash de la app Cignifi: fondo azul oscuro sólido, logo blanco centrado). Como el logo de FrontOne es dinámico (cada empresa sube el suyo desde escritorio, `Configuracion.Empresa.Logo`), no hay forma de fijar un color de marca a mano en el código — se optó por **extraer el color de fondo del propio logo en tiempo real**, no hardcodear una paleta.

- **Dependencia nueva**: `androidx.palette:palette-ktx:1.0.0`.
- **`LoginViewModel.calcularColorFondo(bitmap)`**: usa `Palette.from(bitmap).generate()`, con orden de prioridad `darkVibrantSwatch → darkMutedSwatch → dominantSwatch → mutedSwatch → ColorFondoFallback` (azul `0xFF1A1F5C` fijo). Prioriza tonos oscuros porque la mayoría de logos corporativos son claros/blancos sobre fondo transparente — un fondo oscuro sacado del propio logo los hace resaltar, mismo efecto que la referencia de Cignifi. `EstadoLogo.Disponible` ahora carga también `colorFondo: Color`, no solo la imagen.
- **`ui/login/LoginSplashScreen.kt`** (nuevo): `Box` a pantalla completa con `background(colorFondo)`, logo centrado a `220.dp` con fade-in (`animateFloatAsState`, 500ms). Mientras el logo no ha resuelto (`Cargando`) o si no hay ninguno (`NoDisponible`), usa el mismo `ColorFondoFallback` — nunca se ve un fondo blanco/plano en el splash.
- **`MainActivity.kt`** reestructurado con un `enum Pantalla { SPLASH, LOGIN, CONFIGURACION }` y `Crossfade` entre las tres (antes era un `if` simple de 2 estados). `LoginViewModel` ahora se obtiene una sola vez arriba (`hiltViewModel()` en `MainActivity`, no dentro de `LoginScreen`) y se comparte entre el Splash y el Login — así la consulta del logo no se dispara dos veces. El splash espera a que `estadoLogo` deje de ser `Cargando` (con tope de 2.5s por si la red está lenta, para no dejarlo trabado) + 400ms extra para que la transición no se sienta como parpadeo si el logo carga muy rápido.

**Confirmado por el usuario (2026-08-07), y revertido**: probó con el logo real de Fronterra (ícono geométrico + texto "Fronterra" en gris oscuro semitransparente) — el color que extraía `Palette` quedaba casi idéntico al gris del texto del propio logo, haciendo el texto ilegible contra su propio fondo "a juego". El usuario pidió abandonar la extracción dinámica y usar en su lugar el azul de marca de FrontOne, en degradado (para verse más profesional que un color plano).

**Fix aplicado**: se quitó por completo la extracción de color —
- `app/build.gradle.kts`: se eliminó la dependencia `androidx.palette:palette-ktx`.
- `LoginViewModel.kt`: `EstadoLogo.Disponible` vuelve a ser solo `imagen` (sin `colorFondo`), se quitó `calcularColorFondo`/`ColorFondoFallback`.
- `ui/theme/Theme.kt`: se agregaron dos colores públicos nuevos, `AzulSplashOscuro` (`0xFF141B4D`, índigo profundo) y `AzulSplashClaro` (alias del `AzulFrontOne` ya existente) — públicos a propósito para que el splash no duplique códigos de color sueltos.
- `ui/login/LoginSplashScreen.kt`: el fondo ahora es un `Brush.linearGradient(listOf(AzulSplashOscuro, AzulSplashClaro))` diagonal, fijo — ya no depende en absoluto del `estadoLogo`.

**Trade-off documentado**: un degradado fijo es más predecible y elimina el riesgo de contraste que tuvo la extracción dinámica, a costa de no adaptarse solo si el logo cambia de colores en el futuro (si algún día se sube un logo con colores muy distintos al azul de marca, alguien tendría que revisar el contraste a mano). Se aceptó ese trade-off explícitamente — para esta app de una sola empresa (Fronterra), un color de marca fijo tiene más sentido que una heurística automática que ya demostró fallar con el logo real.

**Confirmado por el usuario, y ajustado de nuevo**: el degradado en sí quedó bien (buena calidad visual, el ícono del logo resalta), pero el texto "Fronterra" seguía sin verse nítido flotando directo sobre el azul — mismo problema de fondo, versión atenuada: el gris oscuro semitransparente del texto no tiene buen contraste contra **ningún** color medio-oscuro, sea el extraído por Palette o el azul de marca fijo. El usuario lo resumió tajante: "no se tiene que confundir nada".

**Fix final — tarjeta blanca**: en vez de seguir peleando con el color de fondo, el logo ahora vive dentro de una `Surface` blanca propia (esquinas redondeadas `28.dp`, sombra `12.dp`, padding `32.dp` alrededor de la imagen a `180.dp`), centrada sobre el degradado azul. El logo de Fronterra se diseñó para fondo claro — ponerlo sobre blanco es la única solución que garantiza legibilidad **sin importar qué logo se cargue en el futuro** desde `Configuracion.Empresa`, a diferencia de las dos soluciones anteriores (extracción dinámica, degradado fijo) que dependían de que el logo tuviera colores compatibles con lo que fuera el fondo. Patrón visual común en splash screens corporativos (tarjeta blanca con logo sobre color de marca) — no es una idea nueva, es el estándar de facto para este problema exacto.

**Lección de la secuencia completa (3 iteraciones para llegar aquí)**: cuando un logo con texto de bajo contraste tiene que verse bien sobre CUALQUIER fondo posible, la respuesta correcta no es afinar el color de fondo — es dejar de poner el logo directo sobre un fondo de color y darle su propio contenedor claro. Vale la pena tenerlo presente si se repite este patrón en otra pantalla del proyecto (ej. si el Login mismo, no solo el Splash, llegara a necesitar el logo sobre un fondo de color en vez de blanco).

**Confirmado por el usuario**: la tarjeta blanca funcionó — pidió después ensancharla ("más rectángulo que un cuadrado"). Se cambió `Surface` a ancho fijo `300.dp` con la imagen a `100.dp` de alto (`ContentScale.Fit`, padding horizontal `32.dp`/vertical `20.dp`) en vez de depender del tamaño intrínseco de la imagen + padding uniforme — quedó una tarjeta horizontal en vez de casi cuadrada. Confirmado que se ve bien.

## Iteración: duración del splash

Petición del usuario en dos pasos: primero "ponle 4 segundos" (duración fija) → se cambió el `LaunchedEffect` de MainActivity a un `delay(4000)` simple, sin esperar al logo. Después pidió volver al comportamiento anterior (esperar a que el logo resuelva, con un tope) pero con el tope en 4s en vez de los 2.5s originales — se restauró `withTimeoutOrNull(4000) { estadoLogo.first { it !is Cargando } } + delay(400)`. Resultado: el splash dura entre ~0.4s (logo rápido) y ~4.4s (tope), nunca fijo, nunca más de eso.

## Iteración: LoginScreen — layout "header" (logo chico arriba-izquierda + ajustes arriba-derecha)

El usuario pidió modernizar `LoginScreen`: logo grande centrado → logo chico en la esquina superior izquierda, y mover el ícono de ajustes (antes un `FloatingActionButton` superpuesto desde `MainActivity`, abajo a la derecha) a esa misma zona superior, junto al logo.

- **`LoginScreen.kt`** reestructurado: `Column` raíz con un `Row` superior (`LogoEmpresaChico` a la izquierda, `Spacer(weight=1f)`, `IconButton` de ajustes a la derecha — mismo patrón visual que un header de app típico) y debajo un `Column(weight(1f))` centrado con el título/subtítulo/tarjeta de campos, igual que antes pero ya no compitiendo por espacio con un logo grande.
- **`LogoEmpresaChico`** (nuevo composable privado, reemplaza a `LogoEmpresa`): caja **rectangular** `36.dp × 130.dp` (no cuadrada — el logo real es mucho más ancho que alto, una caja cuadrada lo dejaría minúsculo al ajustar por el lado corto), `Image` con `Modifier.fillMaxHeight()` y `contentAlignment = CenterStart`.
- **Nuevo parámetro `onConfiguracionClick: () -> Unit`** en `LoginScreen` — el ícono de ajustes ya no es un overlay externo (`Box` + `IconButton` posicionado desde `MainActivity`), es parte del propio layout de la pantalla. `MainActivity.kt` se simplificó: la rama `Pantalla.LOGIN` ahora es solo `LoginScreen(viewModel = ..., onConfiguracionClick = { pantalla = CONFIGURACION })`, sin `Box`/`Alignment`/`IconButton` propios — esos imports se quitaron de `MainActivity.kt`.
- **Sin problema de contraste esta vez**: el fondo del login es `MaterialTheme.colorScheme.background` (`FondoClaro`, casi blanco) — el logo con texto gris oscuro se lee bien ahí sin necesitar tarjeta propia, a diferencia del splash sobre azul.

**Corrección inmediata del usuario**: la interpretación de "el icon button donde mismo" fue incorrecta — el ícono de ajustes debía quedarse **abajo a la derecha** (como estaba desde el principio), solo el logo se movía arriba. Se corrigió `LoginScreen.kt`: el `Row` superior ahora solo contiene `LogoEmpresaChico` (se quitó el `Spacer`/`IconButton` de ahí, y el import de `Spacer` que quedó sin uso). El contenido se envolvió en un `Box` exterior (`Scaffold` → `Box` → `Surface`+`Column` de contenido, más un `IconButton` hermano posicionado con `Modifier.align(Alignment.BottomEnd).padding(24.dp)`) — mismo patrón de overlay que antes vivía en `MainActivity`, ahora dentro de la propia `LoginScreen` (el parámetro `onConfiguracionClick` se conserva, solo cambió dónde se dibuja el botón que lo dispara).

**Gotchas de compilación repetidos en esta secuencia** (mismo síntoma ya documentado arriba, distinto archivo): `import androidx.compose.foundation.layout.weight` colado de nuevo por autocompletado del IDE en `LoginScreen.kt` (mismo fix: quitarlo, es miembro de `RowScope`, no se importa) y un `Unresolved reference 'width'` porque a diferencia de `weight`, `width` sí es una función real que sí necesita `import androidx.compose.foundation.layout.width` — se me olvidó agregarlo al escribir `LogoEmpresaChico`. Distinción a recordar: `weight`/`align` (miembros de scope, nunca se importan) vs. `width`/`height`/`size`/`padding` (funciones de extensión de nivel de paquete, sí se importan).

**Confirmado por el usuario**: el login real funcionó ("¡Bienvenido, Manager FrontOne!" visible en pantalla, con usuario `manager`) — pidió dos ajustes de texto ("quitar el título 'FrontOne'" y mover el subtítulo dentro de la tarjeta, arriba del campo Usuario, con el color de marca). Ambos aplicados: se quitó el `Text("FrontOne")`, y "Inicia sesión con tu usuario de FrontOne" se movió dentro del `Card`, en negritas y `MaterialTheme.colorScheme.primary`.

## Iteración: rediseño completo del login sobre mockup del usuario

El usuario mandó un mockup propio (imagen) con un diseño bastante más elaborado: cabecera azul con un chip blanco con "Fronterra" (texto de referencia — aclaró explícitamente que ahí va el **logotipo real**, no texto), título grande "Bienvenido a FrontOne", subtítulo, un círculo decorativo con iconitos agrícolas en la esquina superior derecha, y una tarjeta blanca de esquinas redondeadas superpuesta a la cabecera con: campos "USUARIO"/"CONTRASEÑA" estilo *filled* (fondo gris claro, sin borde) con etiqueta arriba en vez de flotante, un toggle "Ver"/"Ocultar" contraseña, checkbox "Recordarme" + link "¿Olvidaste tu clave?", botón azul de ancho completo, y un banner de confirmación verde con fondo tintado (no solo texto de color).

**Simplificación consciente y documentada**: el patrón decorativo de iconitos agrícolas dentro del círculo (tractores/árboles/etc.) no se replicó — es ilustración vectorial a mano, fuera de alcance razonable para esta iteración. Se sustituyó por dos círculos translúcidos simples (`Color.White.copy(alpha=0.06f/0.08f)`) que dan la misma sensación de profundidad/textura sin necesitar un set de íconos agrícolas curado.

### Implementación

- **Estructura de 2 secciones**: `Column(fillMaxSize)` con (1) `Box` cabecera de `280.dp` de alto, fondo `Brush.verticalGradient(AzulFrontOne → AzulSplashOscuro)`, y (2) `Surface` de contenido con `Modifier.weight(1f).offset(y = -24.dp)` y `shape = RoundedCornerShape(topStart=28.dp, topEnd=28.dp)` — el offset negativo hace que la tarjeta blanca "muerda" 24dp de la cabecera azul, logrando el efecto de superposición del mockup sin necesitar coordenadas absolutas.
- **Chip del logo**: `Surface` blanca en forma de píldora (`RoundedCornerShape(50)`) conteniendo `LogoEmpresaChico` (reducido a `22dp × 100dp`, más chico que la versión anterior porque ahora vive dentro de un chip, no suelto en un header) — este es el punto donde el mockup decía "Fronterra"; ahí va el logo real traído de `Configuracion.Empresa`, igual que en el resto de la pantalla.
- **Campos "filled"**: se cambió de `OutlinedTextField` a `TextField` con `TextFieldDefaults.colors(...)` — contenedor gris claro (`0xFFF2F3F7`) en los 3 estados relevantes (focused/unfocused/disabled) e indicador (la línea inferior que trae `TextField` por default) puesto en `Color.Transparent` en los 3 estados, para que no se vea ni la línea ni el borde — solo el fondo gris redondeado. Etiqueta "USUARIO"/"CONTRASEÑA" ahora es un `Text` en mayúsculas aparte, arriba del campo (no el parámetro `label` de Material3, que sería flotante).
- **Toggle mostrar/ocultar contraseña**: nuevo estado local `mostrarPassword`, controla `visualTransformation` (`PasswordVisualTransformation()` vs `VisualTransformation.None`) — el `trailingIcon` del campo es un `TextButton` con el texto "Ver"/"Ocultar" (igual que el mockup, no un ícono de ojo).
- **"Recordarme"/"¿Olvidaste tu clave?"**: agregados como **solo visuales, sin funcionalidad** — mismo criterio de todo el login hasta ahora (visual primero, conectar después). El `Checkbox` no persiste nada (estado local que se pierde al recomponer/cerrar la app) y el link no tiene acción real, documentado con un comentario `TODO` en el propio código.
- **`BannerFeedback`** (composable nuevo, reemplaza el `Text` de color plano que había antes para error/éxito): `Surface` con esquinas redondeadas y fondo tintado — verde (`0xFFE3F3E3`/`0xFF2E7D32`) para éxito, `errorContainer`/`onErrorContainer` del theme para error. Mismo patrón visual que el banner del mockup.
- Ícono de ajustes: sin cambios de posición (sigue abajo a la derecha, ver iteración anterior), solo se quitaron los imports que dejaron de usarse (`Card`, `CardDefaults`, `OutlinedTextField` ya no se usan en este archivo, reemplazados por `TextField`/`Surface`).

**Corrección del usuario tras ver el resultado**: "necesito que sea igual al otro" — el primer intento se alejó del mockup en dos puntos concretos: (1) usé campos *filled* (fondo gris, sin borde) con etiqueta en mayúsculas arriba, pero el mockup usa el estilo nativo de `OutlinedTextField` (borde + etiqueta flotante que cruza el borde) con un punto de color adentro; (2) usé círculos decorativos azules translúcidos, pero el mockup usa un círculo **verde** sólido. Ambos corregidos:

- Los campos volvieron a `OutlinedTextField` (con `label = { Text("Usuario") }`/`"Contraseña"` — el cruce de la etiqueta sobre el borde es comportamiento nativo de Material3, no hay que armarlo a mano) + `leadingIcon = { PuntoIndicador() }` (círculo sólido `10.dp`, verde, composable nuevo). Se quitaron los `Text` de etiqueta en mayúsculas y el `TextFieldDefaults.colors` custom que ya no aplica.
- Círculo decorativo: uno solo (antes eran dos azules), verde (`VerdeFronterra = 0xFF6FA828`, tomado a ojo del tono verde del ícono del logo de Fronterra — no es un color de marca confirmado, es una aproximación), `alpha=0.55f`, sin el patrón de iconitos agrícolas (sigue siendo la misma simplificación ya documentada, deliberada).
- `VerdeFronterra` es una constante privada a nivel de archivo, reutilizada tanto en el círculo como en `PuntoIndicador()` — un solo lugar si hay que ajustar el tono después.

Build/verificación visual en vivo: pendiente de que el usuario compile y confirme que ahora sí coincide con su mockup.

## Iteración: implementación exacta desde el archivo de diseño real (DesignSync)

El usuario tenía el diseño completo hecho en su propio proyecto de claude.ai/design (no un boceto/imagen aproximada como las iteraciones anteriores) y pidió importarlo directo. Se usó la herramienta **DesignSync** (`get_project`/`list_files`/`get_file`) contra el proyecto `217870ed-1a06-48df-a6b0-0d6023e7d977` ("Rediseño login profesional") — se leyó `Login FrontOne.dc.html` (el archivo `.dc.html` es el formato de prototipo interactivo de Claude Design, HTML+plantilla con un bloque de JS de estado) y `support.js` (confirmado: solo el runtime genérico del formato `.dc.html`, sin nada de diseño específico).

Con el HTML/CSS exacto disponible, se reemplazó toda la aproximación visual anterior por una traducción literal:

- **Patrón de íconos agrícolas real** (`PatronAgricola.kt`, archivo nuevo): las iteraciones anteriores habían simplificado el círculo decorativo del diseño a un color liso, documentado explícitamente como "fuera de alcance, es ilustración a mano". Con el SVG real disponible, ya no era cierto — se tradujeron los 4 íconos (aguacate, árbol, granero, tractor) coordenada por coordenada desde los `<path>`/`<ellipse>`/`<circle>` del SVG original a `DrawScope` de Compose (`drawOval`/`drawCircle`/`drawPath`/`drawLine` dentro de un bloque `scale(size/24f)` para poder copiar las coordenadas del viewBox `24×24` tal cual, sin recalcular cada punto a mano). `PatronAgricolaCircular` tilea estos íconos en un grid (7×7=49 celdas para el círculo grande, 5×5=25 para el chico) recortado en círculo (`clip(CircleShape)`), cicla aguacate→árbol→granero→tractor por índice `%4`, con un `offsetCiclo` para que el círculo chico arranque desplazado (igual que el diseño original, que usa `(i+2)%4` para el segundo grid).
- **Cabecera**: gradiente de 3 paradas exacto (`#4E6D9C→#3D5A82→#2F4766`), círculo grande verde aguacate (`#8CC63F` alpha .4, offset `x=70dp,y=-40dp`, `210dp`) y círculo chico blanco translúcido (alpha .1, offset `x=-100dp,y=165dp`, `150dp`), más 6 puntos decorativos sueltos en las posiciones exactas del CSS (`PuntoDecorativo`, helper nuevo).
- **Chip del logo**: en el diseño original decía "Fronterra" como texto, pegado al borde izquierdo (`left:0`), redondeado **solo del lado derecho** (`border-radius:0 14px 14px 0` — no es una píldora centrada como se había hecho en iteraciones previas). Se mantuvo la posición/forma exacta pero con el logo real adentro (instrucción del usuario de una iteración anterior, sigue vigente: "donde dice Fronterra va el logotipo").
- **Estructura de layout corregida**: se descubrió, al leer el CSS completo, que el diseño NO es "header con `Column` empujado a un lado + tarjeta con `weight(1f)` superpuesta" (como se había armado a ojo en iteraciones previas) — es un **flujo vertical normal** (`header` con altura fija de fondo absoluto + `tarjeta` como hijo de flujo normal justo después, con un pequeño solape vía margen negativo + `Spacer(weight(1f))` empujando el pie hacia abajo, más el ícono de ajustes como fila normal al final, no un botón flotante superpuesto). Se reestructuró `LoginScreen.kt` completo sobre este modelo.
- **Campos de captura reescritos como componentes 100% custom** (`CampoFrontOne`, nuevo): ni `TextField` (filled) ni `OutlinedTextField` (borde con label flotante) del intento anterior correspondían exactamente al diseño — el diseño usa relleno **y** borde a la vez (`background:#F4F5F9` + `border:1.5px solid #E4E6EF`) con la etiqueta **afuera** del campo (no flotante, `Text` en mayúsculas arriba) — un híbrido que ningún componente de Material3 hace de fábrica. Se armó a mano con `Row` + `BasicTextField` + placeholder manual (`Text` condicional cuando `valor.isEmpty()`). El punto verde indicador (`8dp`) solo aparece en el campo Usuario, no en Contraseña (así es en el HTML original).
- **Checkbox custom** (`CasillaFrontOne`, nuevo): el diseño usa un cuadrado redondeado (`6dp` de radio) con un `✓` de texto blanco cuando está marcado, no el `Checkbox` circular/nativo de Material3 — se reemplazó.
- **`BannerFeedback`** actualizado a los colores exactos del diseño para el estado de éxito (`#F2F8E9`/`#7AB52E`/`#43602A`); el estado de error no tenía variante en el diseño (el prototipo solo muestra el mensaje de bienvenida), así que se derivó una paleta roja con el mismo lenguaje visual (`#FCEBEA`/`#D32F2F`/`#B3261E`).

### Desviaciones deliberadas frente al diseño (documentadas en el propio KDoc de `LoginScreen.kt`)

1. **Barra de estado falsa ("12:09" + batería) y barra inferior tipo home-indicator omitidas** — son parte del *marco de teléfono* del mockup (chrome del sistema operativo dentro de la herramienta de diseño), no de la pantalla real de la app. Replicarlas duplicaría/chocaría con la barra de estado real que Android ya dibuja.
2. **Tipografía**: el diseño usa "Manrope" (Google Fonts) — no está empotrada en el proyecto (requeriría agregar archivos `.ttf` como recurso). Por ahora usa la tipografía default del sistema. Pendiente si se pide exactitud tipográfica.
3. **Dos paletas de azul distintas conviven a propósito**: esta pantalla usa `#4E6D9C` y derivados (los del archivo de diseño real), mientras que el splash/tema (`Theme.kt`, `AzulFrontOne = #3949AB`) sigue usando la paleta anterior — son azules distintos. No se unificaron para no alterar un diseño que el usuario ya aprobó explícitamente en su herramienta; unificar queda pendiente si se pide más adelante.

**Lección operativa para sesiones futuras**: cuando el usuario tiene (o puede tener) un diseño real hecho en claude.ai/design, vale la pena preguntar por el link/proyecto **antes** de aproximar visualmente a mano sobre una captura de pantalla — las iteraciones 1-3 de esta secuencia de rediseño del login se hicieron a ojo sobre imágenes, y en cuanto apareció el archivo real quedó claro que varios detalles (relleno+borde combinados en los campos, forma del chip, estructura real del layout, iconografía real de los círculos) no se habían adivinado bien. `DesignSync` (`get_project`/`list_files`/`get_file`) es la herramienta correcta para esto — no hace falta pedirle capturas de pantalla al usuario si tiene el proyecto de diseño accesible.

**Confirmado por el usuario, con 2 ajustes finos tras verlo corriendo**: el chip del logo se veía chico y el título "Bienvenido a FrontOne" chocaba visualmente con el chip (se solapaban). Causa: el `Spacer` que empuja el título hacia abajo (`54.dp`) no alcanzaba a despejar el chip una vez que este se agrandó — el chip vive en `offset(y=60.dp)` con su propio padding, y el título arranca en `padding(top=34.dp) + Spacer`, así que ambos números tienen que coordinarse a mano (no hay layout automático que evite el choque entre dos elementos posicionados de forma independiente). Ajustes:
- Chip: padding interno `18.dp/10.dp → 22.dp/14.dp`; logo `22.dp×100.dp → 30.dp×135.dp` (más grande, más fácil de leer); ícono de respaldo (`NoDisponible`) `20.dp → 26.dp` a juego.
- Spacer antes del título: `54.dp → 96.dp` — con el chip más grande (~54dp de alto ahora, antes ~42dp) hacía falta más separación para que no se tocaran.

Build/verificación visual en vivo: pendiente de que el usuario confirme que ya no chocan.

## Iteración: control de acceso a la app móvil (permiso "AccesoMovil")

Pedido del usuario: una lógica de permisos para decidir qué usuarios de FrontOne pueden entrar a la app Android — con el usuario "manager" (Administrador) teniendo acceso total sin configuración extra. Se investigó primero (agente de exploración sobre el repo raíz) el modelo de `Seguridad` de escritorio antes de diseñar nada, para reusar la infraestructura ya existente en vez de inventar un esquema paralelo.

**Hallazgo clave**: el modelo `Rol → Permiso → Pantalla → Módulo` + `Accion` ya es genérico — no hace falta una tabla nueva. Cualquier "cosa que se pueda permitir o no" en FrontOne ya se modela como una `Pantalla` dentro de un `Módulo`, con las 4 `Accion` estándar (Consultar/Crear/Modificar/Eliminar), sin importar si esa "pantalla" tiene o no una UI real en WinForms. El rol Administrador recibe acceso total a cualquier pantalla nueva automáticamente por el patrón de seed ya establecido (`CROSS JOIN Rol × Pantalla × Accion filtrado a Administrador`), así que "manager" (que ya tiene ese rol desde `003_Seed_Manager.sql`) queda cubierto sin tocar nada especial para él.

**Diseño**: pantalla virtual **`AccesoMovil`** dentro del módulo `Seguridad` existente, sin UI propia en escritorio — es un permiso puro. Se administra desde la pantalla **Permisos** que ya existe (aparece sola como una fila más de la matriz en cuanto se siembra, con las 4 columnas de acción de siempre, aunque en la práctica solo se usa "Consultar"). Ningún código nuevo en `FrontOne.WinForms` — toda la novedad es un `INSERT` de seed + la validación del lado Android.

- **`Database/Seguridad/035_Seed_Pantalla_AccesoMovil.sql`** (repo raíz): mismo patrón exacto que los demás `Seed_Pantalla_X.sql` del proyecto (`021_Seed_Pantalla_ConfiguracionEmpresa.sql` usado como referencia línea por línea) — `INSERT ... WHERE NOT EXISTS` para la Pantalla, luego `Permiso` vía `CROSS JOIN` filtrado a Administrador.
- **`domain/model/PermisoUsuario.kt`** (nuevo): proyección de una fila de `Seguridad.sp_Usuario_ObtenerPermisos` (Modulo/Pantalla/Accion) — el mismo SP que ya usa `SessionContext` en escritorio, sin cambios, Android es solo un consumidor más.
- **`UsuarioPort`/`UsuarioSqlServerAdapter`**: ganaron `obtenerPermisos(usuarioId): List<PermisoUsuario>`, llamando ese SP con `@UsuarioId`.
- **`LoginUseCase`**: después de validar la contraseña (mismo flujo de siempre), agrega un paso nuevo — trae los permisos del usuario y exige `Seguridad/AccesoMovil/Consultar` antes de dar el login por exitoso. Si la contraseña es correcta pero falta este permiso, el mensaje es distinto del genérico de "usuario o contraseña incorrectos" (`"Tu usuario no tiene permiso para usar la aplicación móvil. Contacta a tu administrador."`) — a propósito, porque ahí el problema real es otro y el usuario necesita saber cuál.

**QA**: se agregaron los casos `1.7`/`1.8`/`1.9` al Bloque 1 (Seguridad) del tracker (`docs/qa/qa-frontone-tracker.html`) — conceder/quitar el permiso y confirmar el efecto en el login móvil, más el caso de "manager" entrando sin configuración. Artifact republicado (hubo un conflicto de versión con otra sesión que había publicado cambios paralelos — se resolvió publicando forzado desde el archivo del repo, que es la fuente de verdad según el propio flujo documentado en `docs/qa/README.md`; el usuario autorizó explícitamente antes de forzar).

**Ejecutado contra el servidor real (2026-08-07)**, con autorización explícita del usuario: `035_Seed_Pantalla_AccesoMovil.sql` corrido vía `sqlcmd` contra `172.16.1.100\FrontOne` — 1 fila insertada en `Seguridad.Pantalla`, 4 filas en `Seguridad.Permiso` (Consultar/Crear/Modificar/Eliminar para el rol Administrador). Verificado con un `SELECT` de confirmación contra las tablas reales, join completo Rol→Permiso→Pantalla→Módulo→Acción.

**Pendiente**: verificación en vivo desde la app — probar login con "manager" (debe entrar, ya tiene el permiso por ser Administrador) y, si se quiere probar el rechazo, crear un Rol de prueba sin `AccesoMovil` y confirmar el mensaje específico (caso 1.8 del tracker de QA).

## Iteración: unificar el azul del splash con el del login

El usuario notó (correcto) que el splash y el login usaban dos paletas de azul distintas — pendiente que ya se había documentado como "aceptado a propósito" en la iteración del rediseño exacto, pero al verlo corriendo se decidió unificar. Pidió que el fondo del splash sea el mismo degradado que ya usa la cabecera/botón "Iniciar sesión".

- `LoginScreen.kt`: `AzulDiseno1`/`AzulDiseno2`/`AzulDiseno3` (los del diseño real, `#4E6D9C`/`#3D5A82`/`#2F4766`) dejaron de ser `private` — mismo paquete que `LoginSplashScreen.kt`, se reutilizan sin necesidad de moverlas a `Theme.kt` ni duplicar los códigos de color.
- `LoginSplashScreen.kt`: `DegradadoFondoSplash` pasó de `listOf(AzulSplashOscuro, AzulSplashClaro)` (paleta vieja, previa a la importación del diseño) a `listOf(AzulDiseno1, AzulDiseno2, AzulDiseno3)` — mismo degradado de 3 paradas que la cabecera del login, ahora consistente en las dos pantallas.
- `Theme.kt`: se eliminaron `AzulSplashOscuro`/`AzulSplashClaro` (quedaron sin ningún uso). `AzulFrontOne` (`#3949AB`, el color primario de `MaterialTheme`/botones default) **no se tocó** — sigue siendo una paleta aparte de `AzulDiseno1/2/3`; unificar esa también no se pidió, queda fuera de alcance de este ajuste.

Build/verificación visual en vivo: pendiente de que el usuario compile y confirme.

## Iteración: InicioScreen — pantalla post-login (cuadrícula de módulos)

Segunda pantalla traída del mismo proyecto de Design vía DesignSync (`Modulos v3.dc.html`, mismo proyecto `217870ed-...` del login) — cierra el pendiente de "no hay pantalla de inicio real tras el login" que quedaba documentado desde la primera iteración del login.

- **`ui/inicio/IconosModulos.kt`** (nuevo): 8 íconos (Pallets/Embarques/Acopio/Cajas de Campo/Báscula/Inocuidad/Calidad/Reportes), mismo patrón que `PatronAgricola.kt` del login — traducción directa de los `<path>`/`<rect>`/`<circle>` SVG a `DrawScope` con `scale(size/24f)`. Los comandos de arco SVG (`a8 8 0 0 1 16 0`, etc., usados en el escudo de Inocuidad, la caja de Cajas de Campo y el documento de Reportes) no tienen equivalente directo en `Path` de Compose (no hay primitiva de arco elíptico relativo) — se aproximaron con `cubicTo` calculando los puntos de control a mano; visualmente equivalentes para el tamaño de ícono (24dp), no una transcripción matemática exacta del arco SVG.
- **`ui/inicio/InicioScreen.kt`** (nuevo): header ("Fronterra" + avatar circular con la inicial del `Usuario` autenticado, clickable pero sin acción todavía), saludo dinámico ("Hola, {primer nombre}") + fecha/hora actual formateada en español (`DateTimeFormatter` + `Locale("es","MX")`, `java.time` disponible nativo desde API 26 — coincide justo con el `minSdk` del proyecto, no hizo falta desugaring), buscador **funcional** (filtra la cuadrícula de 8 módulos por nombre, `contains(ignoreCase=true)`, sin llamada a red — es un filtro puramente local sobre una lista fija en memoria) y la cuadrícula (`LazyVerticalGrid(GridCells.Fixed(2))`) de tarjetas de módulo.
- **Alcance deliberadamente visual**: de los 8 módulos que aparecen en el diseño, algunos ya son pantallas reales de FrontOne de escritorio (Pallets, Acopio, Cajas de Campo/Almacenes, Reportes) y otros no existen todavía como módulo propio en el ERP (Embarques, Báscula — hoy es solo una pantalla de configuración dentro de Sistema, no un módulo operativo — Inocuidad, Calidad). Se trataron los 8 exactamente igual: `onModuloClick` no navega a ningún lado real todavía, mismo criterio de "visual primero, conectar después" del resto de la app.
- **`MainActivity.kt`**: nuevo estado `Pantalla.INICIO` + `usuarioAutenticado: Usuario?` guardado en el propio `MainActivity` (no en un ViewModel aparte — es estado de navegación de la Activity, no de una pantalla específica). `LoginScreen.onLoginExitoso` ahora sí hace algo real (antes era un callback sin uso, dejado listo a propósito en la iteración del login): guarda el usuario y cambia a `Pantalla.INICIO`.

**Pendiente**: verificación visual en vivo — el usuario va a compilar y comparar contra el diseño real. Ningún módulo es clickeable todavía de verdad (fuera de alcance de esta iteración, es la pantalla siguiente que se decida construir).

**Confirmado por el usuario, con 3 ajustes tras verlo corriendo**:
1. Logo también reemplaza el texto "Fronterra" del encabezado de Inicio (mismo criterio ya aplicado en Login/Splash) — `LogoEncabezado` nuevo, reutiliza el `estadoLogo` que ya trae `MainActivity` (no se vuelve a consultar la base).
2. Encabezado pegado a la barra de estado del sistema — padding superior del `Row` de encabezado subido de `24.dp` a `48.dp`.
3. **Bug real, no cosmético**: la fecha/hora se veía "atrasada" respecto al reloj del teléfono. Causa: `remember { LocalDateTime.now() }` sin ninguna clave de recomposición — se calculaba una sola vez al entrar a la pantalla y quedaba congelada indefinidamente, sin relación con el reloj del sistema. Se corrigió con `LaunchedEffect(Unit)` + `delay(60_000)` en loop, actualizando el texto cada minuto mientras la pantalla esté visible.
4. Logo del encabezado agrandado (`26×115dp → 34×150dp`), a pedido explícito.
5. **Cambio de arquitectura del scroll**: el usuario notó que solo la cuadrícula desplazaba (por usar `LazyVerticalGrid`, que reclama su propio scroll interno), dejando el encabezado/buscador fijos arriba — con el nuevo padding superior, esto cortaba visualmente la primera fila de tarjetas. Se reemplazó el `Column` raíz por uno con `.verticalScroll(rememberScrollState())`, y el `LazyVerticalGrid` por un grid armado a mano (`chunked(2)` + `Row`/`Column` con `weight(1f)`) — con una lista fija de 8 módulos (nunca crece dinámicamente, solo se filtra localmente) no hace falta la virtualización de un `Lazy*`, y evita por completo el problema de scroll anidado. Ahora toda la pantalla se desplaza como una sola unidad.

Build/verificación visual en vivo: pendiente de que el usuario compile y confirme el scroll unificado.

## Iteración: permisos por rol — qué tarjetas de módulo ve cada usuario

Pedido del usuario, en dos partes ("en FrontOne y en la app"): quería autorizaciones granulares — cada tarjeta de la pantalla de Inicio es un "módulo" que se puede permitir o no por Rol, igual que ya funciona `AccesoMovil` (el gate de "puede entrar a la app o no"), pero uno por tarjeta.

**Diseño**: mismo patrón exacto que `AccesoMovil`, extendido — un módulo nuevo **`Seguridad.AplicacionMovil`** (separado de `Seguridad` a propósito: "¿puede entrar?" es una decisión distinta de "¿qué ve una vez adentro?") con **8 Pantallas**, una por tarjeta, incluyendo la Ñ/acentos exactos del nombre visible (`Pallets`, `Embarques`, `Acopio`, `Cajas de campo`, `Báscula`, `Inocuidad`, `Calidad`, `Reportes`) — el nombre de la Pantalla en SQL es idéntico al texto que ve el usuario en la tarjeta, sin traducir a un slug técnico, así el admin no tiene que adivinar cuál fila de la matriz de Permisos corresponde a cuál tarjeta.

- **`Database/Seguridad/036_Seed_Modulo_AplicacionMovil.sql`**: mismo patrón que `035_Seed_Pantalla_AccesoMovil.sql`, con la diferencia de que las 8 Pantallas se insertan en un solo `INSERT ... SELECT` vía `CROSS JOIN (VALUES (...), (...), ...)` en vez de 8 sentencias sueltas. Ejecutado contra `172.16.1.100\FrontOne`: 1 fila (Módulo) + 8 filas (Pantallas) + 32 filas (Permisos: 8 pantallas × 4 acciones para Administrador). Verificado con `SELECT ... GROUP BY` — las 8 pantallas con 4 permisos cada una para Administrador.
- **`ResultadoLogin.Exitoso` ganó un campo `permisos: List<PermisoUsuario>`** — ya se traían dentro de `LoginUseCase` para validar `AccesoMovil`, así que no hace falta una segunda consulta a `sp_Usuario_ObtenerPermisos` solo para que Inicio sepa qué tarjetas mostrar. Se propagaron en cadena: `LoginUseCase` → `EstadoLogin.Exitoso` (`LoginViewModel`) → `onLoginExitoso: (Usuario, List<PermisoUsuario>) -> Unit` (`LoginScreen`) → estado `permisosUsuario` en `MainActivity` → parámetro `permisos` de `InicioScreen`.
- **`InicioScreen`**: nuevo `modulosPermitidos = remember(permisos) { MODULOS.filter { ... } }`, cruzando `ModuloInfo.nombre` contra `permisos` (`modulo == "AplicacionMovil" && pantalla == nombre && accion == "Consultar"`) — el filtro de búsqueda (`modulosFiltrados`) ahora corre **sobre** `modulosPermitidos`, no sobre la lista completa; una tarjeta sin permiso no aparece ni siquiera si se busca por su nombre exacto.

**QA**: casos `1.10`/`1.11` agregados al Bloque 1 (Seguridad) — "manager" ve las 8 tarjetas sin configurar nada, y quitar el permiso de 2-3 pantallas específicas hace desaparecer solo esas tarjetas. Artifact republicado (mismo flujo de `force: true` ya usado antes, archivo del repo como fuente de verdad).

**Pendiente**: verificación visual en vivo — compilar y confirmar que las 8 tarjetas siguen viéndose con "manager" (ya tiene los 32 permisos), y opcionalmente probar el caso 1.11 quitándole un permiso a un Rol de prueba.

## Iteración: "Recordarme" funcional

El checkbox "Recordarme" del login existía solo como estado visual desde la iteración del rediseño exacto (sin persistir nada). El usuario pidió darle función real, y antes de programar se le preguntó explícitamente qué debía recordar exactamente — dado que las 3 opciones razonables (solo usuario / usuario+contraseña / sesión completa) tienen implicaciones de seguridad muy distintas si el dispositivo se pierde desbloqueado. Eligió **usuario + contraseña**.

- **`app/config/CredencialesRecordadasStore.kt`** (nuevo): mismo patrón exacto que `ConfiguracionConexionStore` — `EncryptedSharedPreferences` respaldado por Android Keystore, archivo separado (`frontone_credenciales_recordadas`, no comparte prefs con la configuración de conexión SQL Server). `obtener()`/`guardar(usuario, password)`/`limpiar()`. Vive en `:app` por la misma razón de siempre (necesita `Context`, el dominio/data no pueden depender de Android).
- **Trade-off de seguridad documentado en el propio KDoc del store**: si el dispositivo se pierde desbloqueado, quien lo tenga puede entrar sin saber la contraseña real (ya queda precargada) — aceptado explícitamente por el usuario a cambio de comodidad. El cifrado en reposo (Keystore) protege contra extraer el archivo de preferencias fuera del dispositivo, no contra un dispositivo desbloqueado en manos de otra persona — mismo tipo de trade-off ya aceptado antes con la clave fija de `CryptoService`.
- **`LoginViewModel`**: ganó `credencialesGuardadas: StateFlow<CredencialesGuardadas?>`, cargado en `init{}` junto con el logo. `iniciarSesion` ahora recibe un tercer parámetro `recordarme: Boolean` — tras un login exitoso, guarda las credenciales si `recordarme=true` o limpia cualquier credencial guardada previamente si es `false` (para que desmarcar el checkbox en una sesión futura sí borre lo guardado, no solo deje de sobreescribir).
- **`LoginScreen`**: nuevo `LaunchedEffect(credencialesGuardadas)` que precarga `usuario`/`contrasena`/`recordarme` **una sola vez**, y solo si los campos siguen vacíos (evita pisar lo que el usuario ya esté escribiendo si la lectura async del store tarda). El botón "Iniciar Sesión" ahora pasa `recordarme` al ViewModel.

**Pendiente**: verificación en vivo — marcar Recordarme, cerrar y volver a abrir la app, confirmar que los campos llegan precargados; desmarcarlo y volver a iniciar sesión, confirmar que la próxima vez ya no se precarga nada.

## Iteración: módulo Pallets — captura completa (alta/edición de encabezado + líneas de detalle)

El equipo fusionó a `main` el módulo Pallets de escritorio (armado de tarimas con producto terminado, consumiendo saldo de Corridas "en proceso"). El usuario pidió captura completa desde el celular, no solo consulta — mismo criterio de las 12 Stored Procedures vigentes de `Produccion.Pallet`/`Produccion.PalletDetalle` (incluidas sus versiones tras los `ALTER` de agosto: producto de referencia por encabezado, bloqueo solo si Completo, presentación Caja/Granel) más `Catalogos.sp_LineaProduccion_Obtener`/`sp_ProductoTerminado_ObtenerTop1000`/`_Buscar`. Plan aprobado en Plan Mode antes de escribir código (dos Explore + un Plan agent, lectura directa de los 6 SQL con las firmas finales).

**Cambio previo indispensable — `SqlRepositoryBase.kt`**: los SPs de Pallets rechazan reglas de negocio con `THROW 50000, 'mensaje en español', 1` (saldo insuficiente, pallet bloqueado, exceso de cajas, etc.). `traducirError` solo traducía 547/2627/2601 — se agregó un branch `50000 -> ex.message ?: "..."`. **Verificado contra el servidor real** antes de construir el resto encima: se creó un SP de prueba (`Produccion.sp_Test_Throw50000`, borrado después) con `THROW 50000, N'Prueba de mensaje de negocio en español.', 1` y se llamó desde un programa Java standalone usando el mismo jar de jTDS (`jtds-1.3.1.jar`) del proyecto — resultado: `errorCode=50000`, `message=[Prueba de mensaje de negocio en español.]`, exactamente el texto sin prefijo raro. Confirma que el patrón es seguro para todo módulo de escritura futuro, no solo este.

**`:domain` — modelos/puertos/casos de uso nuevos**:
- `model/Pallet.kt` (`Pallet` + enum `EstatusPallet` 1-6), `PalletDetalle`, `LoteEnProceso` (fila del picker de Corridas en proceso).
- `model/ProductoTerminado.kt` (subconjunto de columnas, solo lo que consume Pallets — el catálogo completo se queda en escritorio) + enum `PresentacionProducto` (Caja/Granel).
- `model/LineaProduccion.kt` (catálogo simple).
- `port/PalletPort.kt`, `ProductoTerminadoPort.kt`, `LineaProduccionPort.kt`.
- 12 casos de uso en `usecase/` (uno por operación, clases planas). El más delicado: **`AgregarLineaPalletUseCase`** replica una regla real de `PalletService.AgregarLineaAsync` que no vive en ningún SP — si ya existe una línea del mismo Pallet con el mismo `CorridaId`+`ProductoTerminadoId`, no se duplica la fila, se **suma** a la existente (Cajas o Kilogramos según la Presentación) vía `actualizarDetalle`. Sin esto la app hubiera divergido del comportamiento real de escritorio.

**`:data` — adaptadores nuevos**: `PalletSqlServerAdapter.kt` (10 SPs), `ProductoTerminadoSqlServerAdapter.kt` (2 SPs), `LineaProduccionSqlServerAdapter.kt` (1 SP) — mismo patrón que `UsuarioSqlServerAdapter`. Primeras veces que el proyecto llama `ejecutarProcedimiento` para SPs sin result set (`Bloquear`/`Eliminar`/`Actualizar*`): `leerResultado = { it.execute() }` con `T = Unit` — Kotlin descarta el `Boolean` de `execute()` sin problema porque el tipo esperado del lambda es `Unit`. Se agregaron 3 helpers de extensión privados a `PalletSqlServerAdapter.kt` (`CallableStatement.setNullableInt/setNullableBigDecimal`, `ResultSet.getNullableInt`) para los parámetros/columnas opcionales (`Cajas`/`Kilogramos` son mutuamente excluyentes y nullable desde `009_Alter_PalletDetalle_Granel.sql`).

**`:app` — DI, navegación, pantallas**:
- `di/DataModule.kt`: 3 `@Provides` de puerto→adaptador + 12 de caso de uso.
- **Sin Navigation Compose todavía** (mismo criterio ya documentado en el KDoc de `MainActivity.kt`): un solo `Pantalla.PALLETS` nuevo en el enum de `MainActivity`, un branch nuevo en su `Crossfade` que renderiza `PalletsHostScreen`. La sub-navegación Lista↔Captura vive encapsulada **dentro** del módulo (`ui/pallets/PalletsHostScreen.kt`), con su propio `sealed interface RutaPallets { Lista, Captura(palletId, instancia) }` + `Crossfade` interno — `MainActivity` no sabe nada de esa sub-navegación.
- **Trampa encontrada y corregida antes de que fuera bug real**: `hiltViewModel()` sin `key` explícito reutiliza la misma instancia de ViewModel entre visitas a la misma pantalla mientras no haya Navigation Compose de por medio (el `ViewModelStore` vive en la Activity, no se destruye solo porque `Crossfade` quite el composable). Eso hubiera dejado pegada la data de la visita anterior al reabrir "Nuevo pallet" una segunda vez, o al pasar de un Pallet a otro. Se resolvió agregando `instancia: Long = System.nanoTime()` a `RutaPallets.Captura` y pasando `hiltViewModel(key = "pallet-captura-$palletId-$instancia")` — cada visita a Captura, incluidas dos altas seguidas, obtiene una instancia nueva de `PalletCapturaViewModel`.
- `ui/pallets/`: `PalletsListaViewModel`/`PalletsListaScreen` (filtro por Estatus **en memoria** — no existe SP de filtro, `sp_Pallet_Obtener` solo filtra por `@Id`), `PalletCapturaViewModel`/`PalletCapturaScreen` (orquestador: mientras `palletId == null` -alta nueva sin guardar- la sección de líneas de detalle no se puede mostrar, exactamente la restricción real de `PalletEditarForm.cs`), `PalletEncabezadoSeccion` (Línea de Producción, Es Mixto, Producto condicional, Peso Real, Guardar — todo deshabilitado si `bloqueado`), `PalletDetalleLista` (fila deshabilitada si `!loteEnProceso`), `DialogoLineaDetalle` (picker de Lote en Proceso solo al agregar nunca al editar, producto fijo si no-mixto o buscable si mixto, campo condicional Cajas vs Kilogramos según `PresentacionProducto`), `EstatusPalletUi.kt` (extensiones `etiqueta()`/`color()` compartidas entre Lista y Captura).
- Confirmaciones de Bloquear ("acción irreversible") y Eliminar (libera kilos de sus lotes) vía `AlertDialog`, mismo criterio de "todo evento de eliminar pregunta antes" del proyecto.
- `InicioScreen.kt`: `onModuloClick` ahora navega de verdad para "Pallets" (los otros 7 módulos siguen sin pantalla real detrás).

**Fuera de alcance, decisión consciente** (documentada también en el plan aprobado): "Pallet Neutro" (flujo de Corridas, no de Pallets), lectura de báscula física (Peso Real = campo numérico manual), impresión de Papeleta/Etiquetas, auditoría (`Auditoria.Registro` — Android todavía no audita nada en ningún módulo).

**Pendiente**: no se pudo compilar con Gradle desde este entorno de trabajo (el daemon/worker de Gradle 9.0.0 falla con `java.io.IOException: Unable to establish loopback connection` sin importar el JDK probado — 17/21/25/26 — parece una restricción de red del entorno, no un problema de versión; el resto del proyecto siempre se compiló/verificó desde Android Studio directamente).

### Fix 1: `ExposedDropdownMenu`/`menuAnchor()` son extensiones de `ExposedDropdownMenuBoxScope`, no funciones top-level

Tras sincronizar en Android Studio salió `Unresolved reference 'ExposedDropdownMenu'` en `DialogoLineaDetalle.kt`. Se extrajo `material3-android-1.4.0.aar` (resuelto por `compose-bom:2026.06.01`) y se confirmó vía `classes.jar`: `ExposedDropdownMenu` es un miembro de `ExposedDropdownMenuBoxScope` (`ExposedDropdownMenuBoxScope$ExposedDropdownMenu$2.class`), no una función top-level de `androidx.compose.material3` — **exactamente la misma trampa ya documentada para `weight`/`align`** (RowScope/ColumnScope/BoxScope), solo que en un API nuevo. Se corrigió quitando el `import androidx.compose.material3.ExposedDropdownMenu` de `DialogoLineaDetalle.kt` y la referencia calificada `androidx.compose.material3.ExposedDropdownMenu(...)` de `PalletEncabezadoSeccion.kt` — la llamada sin calificar dentro del lambda de `ExposedDropdownMenuBox { }` resuelve sola vía el receiver implícito de scope.

### Fix 2: `java.sql.Date/Time/Timestamp.toLocalDate()/toLocalTime()/toLocalDateTime()` no existen en el `java.sql` de Android

Al entrar a Pallets desde un dispositivo real: `NoSuchMethodError: No virtual method toLocalDate()Ljava/time/LocalDate; in class Ljava/sql/Date;`. Causa: el `core-oj.jar` de Android reimplementa `java.sql.Date/Time/Timestamp` (siguen extendiendo `java.util.Date`) pero sin los métodos default de conveniencia hacia `java.time` que sí trae el JDK de escritorio — compila sin avisos porque el compilador solo ve el classpath de escritorio, pero truena en tiempo de ejecución en el dispositivo. Solución en `PalletSqlServerAdapter.kt`: 3 helpers de extensión (`java.sql.Date.aLocalDate()`, `java.sql.Time.aLocalTime()`, `java.sql.Timestamp.aLocalDateTime()`) que parsean el `toString()` de cada tipo — formato fijo garantizado por el contrato JDBC (`yyyy-MM-dd`, `HH:mm:ss`, `yyyy-MM-dd HH:mm:ss[.fffffffff]`), no depende de la plataforma. Cualquier adaptador futuro que lea columnas `DATE`/`TIME`/`DATETIME` debe usar este mismo patrón, nunca los métodos `toLocalX()` directos.

## Iteración: Permisos de Aplicación Móvil — pantalla dedicada en escritorio + granularidad Crear/Modificar/Eliminar en Android

El usuario, tras ver funcionar Pallets, pidió separar los permisos móviles de la matriz general de Permisos ("vamos a tener problemas dentro de Seguridad") y, al preguntarle el motivo concreto, confirmó que además quiere que la app respete Crear/Modificar/Eliminar por módulo, no solo el gate de Consultar que ya existía para las tarjetas de Inicio.

**Investigación previa** (subagente Explore, sin escribir código): confirmó que `Seguridad.Accion` es una tabla global (4 filas: Consultar/Crear/Modificar/Eliminar) y que `sp_Permiso_ObtenerEstructura` la cruza (`CROSS JOIN`) contra **toda** Pantalla — es decir, las 4 acciones ya existen para las 9 pantallas móviles desde que se sembraron (`035`/`036`), sin necesidad de ningún cambio de esquema o seed nuevo. También encontró un precedente exacto de "pantalla de permisos separada por área" ya en el proyecto: `ReportePermisosForm.cs` (permisos de Reportes, aunque con su propio esquema `ReportePermiso` en vez de reutilizar `Seguridad.Permiso` — el usuario explícitamente pidió reutilizar el modelo de datos general, no clonar ese patrón de esquema separado).

**Desktop — `FrontOne.WinForms/Forms/Seguridad/PermisosAplicacionMovilForm.cs`/`.Designer.cs`/`.resx`** (nuevo). Corrección sobre una nota anterior: el subagente Explore que investigó esta iteración reportó que `PermisosForm`/`ReportePermisosForm` no tenían `.resx` — **estaba mal**, sí lo tienen (`Forms/Seguridad/PermisosForm.resx`/`ReportePermisosForm.resx`, el `Glob` que se corrió en su momento no los encontró por el patrón usado). Al compilar la primera versión de esta pantalla sin `.resx` propio, tronó en tiempo de ejecución con `MissingManifestResourceException` — a diferencia de `resources.GetObject` con una *clave* faltante dentro de un `.resx` que sí existe (eso sí regresa `null` sin tronar), `ComponentResourceManager` lanza excepción cuando no hay **ningún** `.resources` embebido para ese nombre de tipo en absoluto. Se corrigió creando `PermisosAplicacionMovilForm.resx` como copia verbatim de `PermisosForm.resx` (mismos dos bloques `_btnGuardar.ImageOptions.Image`/`_btnCerrar.ImageOptions.Image`), seguiendo al pie de la letra la regla de `CLAUDE.md` raíz sobre el ícono de botones CRUD. Mismo patrón exacto que `PermisosForm.cs` (`LookUpEdit` de Rol con botón `+`→`RolesForm`, `GridControl`/`GridView` con columnas booleanas auto-generadas, `_btnGuardar`/`_btnCerrar`), reutilizando `PermisoService.ObtenerMatrizAsync`/`GuardarAsync` **sin ningún cambio** — cero SPs nuevos, cero migraciones. El grid se filtra a las 9 pantallas móviles (`ModuloNombre == "AplicacionMovil"` o `(ModuloNombre == "Seguridad" && PantallaNombre == "AccesoMovil")`).

**Gotcha real evitado**: `PermisoService.GuardarAsync(rolId, filas)` borra **todos** los permisos del rol y vuelve a insertar exactamente lo que se le mande (`sp_Permiso_EliminarPorRol` + `sp_Permiso_Insertar` en bucle, dentro de una transacción) — si esta pantalla nueva le mandara solo las 9 filas móviles editadas, borraría de un plumazo todos los permisos de escritorio de ese rol. Se resolvió guardando `_matrizCompleta` (el resultado íntegro de `ObtenerMatrizAsync`, todas las pantallas) al cargar, editando solo el subconjunto móvil en el grid, y al Guardar reconstruyendo la lista completa: pantallas no-móviles tal como llegaron + las 9 móviles con los valores recién editados — antes de llamar `GuardarAsync`.

**Ribbon**: nuevo `_btnPermisosAplicacionMovil` (`Id = 39`, siguiente libre tras el `38` de `_btnConfiguracionBascula`) en el mismo grupo `_grpUsuariosRoles` que `_btnPermisos`/`_btnReportePermisos`, mismo patrón sin ícono.

**Android — enforcement real de Crear/Modificar/Eliminar**: `domain/model/PermisoUsuario.kt` ganó una extensión `List<PermisoUsuario>.tienePermiso(pantalla, accion, modulo = "AplicacionMovil")`. `permisos` ahora se propaga en cadena hasta donde hace falta: `MainActivity` → `PalletsHostScreen(permisos)` → `PalletsListaScreen` (FAB "+" solo visible con `Pallets/Crear`) y `PalletCapturaScreen` (computa `puedeCrear`/`puedeModificar`/`puedeEliminar` una sola vez con `remember(permisos)`, los reparte hacia abajo): botón Guardar del encabezado condicionado a Crear (alta nueva) o Modificar (edición) vía nuevo parámetro `puedeGuardar` en `PalletEncabezadoSeccion`; botón "Agregar" línea condicionado a Crear, Editar a Modificar, Eliminar a Eliminar, vía nuevos parámetros `puedeCrear`/`puedeModificar`/`puedeEliminar` en `PalletDetalleLista`; íconos Bloquear/Eliminar del `TopAppBar` de Captura condicionados a Modificar/Eliminar respectivamente. Todo enforcement es de UI (oculta/deshabilita el control) — igual que el resto de la app hoy, no hay una segunda capa de validación server-side aparte de las reglas de negocio que ya tiran los SPs vía `THROW 50000`.

**Pendiente**: sincronizar/compilar `FrontOne.sln` en Visual Studio (no se pudo compilar C# desde este entorno de trabajo tampoco), abrir `PermisosAplicacionMovilForm` con el diseñador para confirmar que el grid se ve bien, probar en Android con un Rol de prueba: quitarle "Crear" en Pallets y confirmar que desaparece el FAB "+" y el botón "Agregar línea"; quitarle "Modificar" y confirmar que Guardar (al editar) y Bloquear desaparecen; quitarle "Eliminar" y confirmar que el ícono de basura del pallet y de cada línea desaparecen.

**Ajuste 1 — `MissingManifestResourceException` al abrir `PermisosAplicacionMovilForm`**: el subagente Explore que investigó esta iteración reportó (mal) que `PermisosForm`/`ReportePermisosForm` no tenían `.resx` propio — el `Glob` que corrió no los encontró por el patrón usado, pero sí existen (`Forms/Seguridad/PermisosForm.resx`/`ReportePermisosForm.resx`). A diferencia de una *clave* faltante dentro de un `.resx` que sí existe (`resources.GetObject` regresa `null` sin tronar), `ComponentResourceManager` lanza excepción cuando no hay **ningún** `.resources` embebido para ese nombre de tipo. Se corrigió creando `PermisosAplicacionMovilForm.resx` como copia verbatim de `PermisosForm.resx` (mismos dos bloques `_btnGuardar.ImageOptions.Image`/`_btnCerrar.ImageOptions.Image`).

**Ajuste 2 — sacar AccesoMovil/AplicacionMovil de la matriz general de Permisos**: el usuario pidió esto de inmediato al ver la pantalla general seguía mostrando las 9 pantallas móviles mezcladas con las de escritorio (opción "Pantalla nueva + reorganizar" de la pregunta original, no la más conservadora que se había implementado primero). `PermisosForm.cs` ahora sigue el mismo patrón espejo que `PermisosAplicacionMovilForm`: guarda `_matrizCompleta` (resultado íntegro de `ObtenerMatrizAsync`) y filtra `_filas` (lo que se ve/edita en el grid) a **excluir** `EsPantallaMovil(...)` — mismo predicado duplicado en ambos forms (`ModuloNombre == "AplicacionMovil"` o `Seguridad`/`AccesoMovil`). Al guardar, reconstruye la lista completa concatenando las filas móviles de `_matrizCompleta` (intactas, ese form ya no las toca) con las filas de escritorio recién editadas — evita el mismo problema de `GuardarAsync` borrando todo el rol. Desde esta iteración, `PermisosForm` y `PermisosAplicacionMovilForm` son mutuamente excluyentes por conjunto de pantallas: ninguna pantalla aparece en ambas.

## Iteración: reconciliación con el PR de un compañero (`Seguridad.MovilPermiso`)

Un compañero abrió su propio PR (#46, "Separa permisos de FrontOne.Android en Seguridad.MovilPermiso y renumera Seguridad.Pantalla") en paralelo a esta sesión, resolviendo el mismo problema de fondo (permisos móviles mezclados con los de escritorio) con un diseño distinto y mejor: en vez de reutilizar `Seguridad.Pantalla`/`Permiso` con un filtro manual (lo que se había hecho en la iteración anterior), creó una tabla dedicada `Seguridad.MovilPermiso` — mismo patrón que ya existía para `Seguridad.ReportePermiso` — y migró/renumeró `Seguridad.Pantalla` para no dejar huecos de Id. Se fusionó a `main` antes que esta rama, así que `git pull` lo trajo directo sin conflictos de merge.

**Verificado exhaustivamente que es 100% compatible con todo lo construido en Android esta sesión**, sin tocar una sola línea:
- El nuevo SP `Seguridad.sp_Usuario_ObtenerMovilPermisos` (`Database/Seguridad/038_Schema_SP_MovilPermiso.sql`) se diseñó a propósito con la misma forma de salida (`Modulo`/`Pantalla`/`Accion`, una fila por acción otorgada) que el genérico `sp_Usuario_ObtenerPermisos` — el compañero ya actualizó `UsuarioSqlServerAdapter.kt` para concatenar ambos resultados (`obtenerPermisos` = genérico + móvil). El modelo `PermisoUsuario`, la extensión `tienePermiso()`, el gate de `LoginUseCase`, el filtro de tarjetas de `InicioScreen` y los permisos granulares Crear/Modificar/Eliminar de Pallets construidos en la iteración anterior siguen funcionando sin cambios.
- `PermisosForm.cs` (escritorio) quedó revertido a su versión simple original — correcto, ya no hace falta el filtro `EsPantallaMovil` que se había agregado, porque las pantallas móviles ya no existen físicamente en `Seguridad.Pantalla` (se migraron a la tabla nueva).
- La migración de datos (`039_Migrar_MovilPermiso_Renumerar.sql`) preserva los permisos ya otorgados por rol y ya se corrió/verificó en producción (172.16.1.100) según el mensaje de commit del compañero.

Compilación de verificación tras el pull: `dotnet build FrontOne.WinForms/FrontOne.WinForms.csproj` — 0 errores (solo warnings esperados de licencia DevExpress trial y NuGet). Gradle de Android sigue sin poder correr desde este entorno de trabajo (`Unable to establish loopback connection`, probado con 5 JDKs distintos — restricción de red del entorno, no del proyecto).

## Iteración: pantalla de Lista de Pallets sobre el diseño real (`Pallets.dc.html`)

Mismo flujo que Login/Inicio: diseño real traído vía `DesignSync` del proyecto `217870ed-1a06-48df-a6b0-0d6023e7d977`, archivo `Pallets.dc.html`. Se reescribió `PalletsListaScreen.kt` pixel-preciso al mockup (header con botón redondo + título, buscador con borde `#E6E8F0`, chips de filtro por Estatus con sombra/color condicional, tarjetas con banda superior de color de Estatus + cuerpo con Línea/badge/producto/cajas·kg·SAP, FAB `#4E6D9C`), y el color de `EstatusPallet.VACIO` se ajustó de `#9A9EB0` a `#6B6F82` para calzar exacto con `STATUS_COLORS` del mockup.

**Decisión de integración, no tomada literal del mockup**: el archivo de diseño incluye una segunda pantalla "Nuevo pallet" que es solo un placeholder visual ("Creación de pallet — Próximamente") — el diseñador nunca cubrió el flujo de captura completo en Design porque es demasiado complejo para ese formato. Implementar eso literalmente hubiera sido una regresión real: ya existe `PalletCapturaScreen` completo y funcional (alta/edición, líneas Caja/Granel, Bloquear/Eliminar, permisos granulares). El FAB y el tap en cada tarjeta siguen enrutando a `PalletCapturaScreen` real, no al placeholder del mockup — solo se tomó del archivo de diseño la pantalla de Lista.

También se agregó búsqueda por texto en memoria (folio o producto, `contains` case-insensitive) — el mockup la trae pero el `PalletsListaViewModel` original no tenía ese campo; se implementó como estado local del Composable, igual que el filtro de `InicioScreen`, sin tocar el ViewModel.

Bug real encontrado y corregido durante la escritura (antes de que llegara a Android Studio): un intento inicial de aplicar `clickable` sobre `Modifier` sin receiver (`androidx.compose.foundation.clickable(onClick = onClick)` como llamada suelta) — `clickable` es una función de extensión de `Modifier`, no se puede invocar así. Corregido a `.clickable(onClick = onClick)` encadenado en la cadena de modificadores.

**Pendiente**: sincronizar en Android Studio (Gradle sigue bloqueado en este entorno) y confirmar visualmente contra el mockup: chips de filtro, tarjetas con banda de color por Estatus, buscador, FAB.

## Iteración: ajustes de UX en Pallets — sin "Folio", gestos de regresar, actualización periódica

Tres pedidos puntuales del usuario tras ver el listado con el diseño real:

- **Quitar el texto "Folio" de la tarjeta**: la banda superior de cada tarjeta (`PalletsListaScreen.kt`) y el chip resumen de `PalletCapturaScreen.kt` ahora muestran solo el número (`pallet.folio`), sin el prefijo.
- **Gestos de regresar (swipe), no solo los botones de flecha**: sin Navigation Compose, el gesto/botón físico de regresar de Android por defecto cierra la Activity en vez de navegar dentro de la app — hacía falta `BackHandler` (`androidx.activity.compose`, ya en el classpath vía `activity-compose:1.9.3`) en cada nivel de navegación:
  - `MainActivity.kt`: `BackHandler(enabled = pantalla == Pantalla.CONFIGURACION) { pantalla = Pantalla.LOGIN }`. Login/Inicio se quedan con el comportamiento por defecto (salir de la app), como cualquier pantalla raíz — mismo criterio que ya se documentó para los botones de flecha visibles.
  - `PalletsHostScreen.kt`: dos `BackHandler`, uno por rama de `ruta`, mutuamente excluyentes vía `enabled` (`Lista` → `onVolverAInicioClick()`; `Captura` → volver a `Lista`) — exactamente lo mismo que hacen los botones de flecha ya cableados, solo que disparado por el gesto del sistema en vez de un click.
  - Los diálogos (`AlertDialog`/`DialogoLineaDetalle`) no necesitaron nada nuevo — Compose ya cierra un `Dialog` con el gesto/botón de regresar por su cuenta.
- **Actualización "en tiempo real"**: sin WebSockets/SignalR ni caché offline (regla dura del proyecto: la app es *online-only*), la única forma de que un pallet creado/modificado desde otro dispositivo aparezca sin que el usuario salga y reentre es un *polling* silencioso. Se agregó en dos ViewModels, mismo patrón en ambos: un `while (true) { delay(5_000); ...actualizar sin pasar por Cargando... }` corriendo en `viewModelScope` (se cancela solo al destruirse el ViewModel, no hace falta detenerlo a mano):
  - `PalletsListaViewModel`: refresca la lista completa cada 5s, reemplaza `_estado.value` directamente con `Cargado(...)` — nunca pasa por `Cargando` (no se ve parpadeo) y si el refresco de fondo falla (ej. una desconexión momentánea) se ignora en silencio en vez de tirar la pantalla a `Error` por algo que el usuario no provocó; el filtro/búsqueda del usuario no se toca, corren sobre la lista ya actualizada en la siguiente recomposición.
  - `PalletCapturaViewModel`: reutiliza la función `recargar(palletId)` que ya existía (la misma que usan Guardar/Agregar línea/Bloquear) — cada 5s, si hay un pallet real ya guardado (`palletId != null`), refresca `pallet`/`detalle`. No pisa los campos del formulario de encabezado ni el diálogo de línea en edición porque esos viven como estado local del Composable (`remember`), no en el `StateFlow` del ViewModel — `recargar` solo toca `pallet`/`detalle`.
  - Intervalo elegido (5s) es una decisión propia, no especificada por el usuario ("cada cierto tiempo") — suficientemente frecuente para sentirse "en tiempo real" sin abusar de una conexión SQL Server compartida por todos los dispositivos.

## Iteración: Captura de Pallet sobre el diseño real completo (`Pallets.dc.html` con vista de Detalle)

El usuario actualizó el archivo de diseño en claude.ai/design agregando la vista de Detalle/Captura completa (antes solo traía la Lista + un placeholder "Próximamente"). Se volvió a importar vía `DesignSync` y se reconstruyó toda la captura (`PalletCapturaScreen`, `PalletEncabezadoSeccion`, `PalletDetalleLista`, `DialogoLineaDetalle`) pixel-precisa al mockup, **conservando intactas todas las reglas de negocio reales** ya construidas (permisos granulares, Bloquear/Eliminar, Caja vs Granel, suma de líneas duplicadas, THROW 50000) — el mockup es una simulación en memoria sin esas reglas, solo se tomó de ahí el diseño visual.

**Piezas nuevas compartidas** (`ui/pallets/`):
- `PalletsPaleta.kt`: colores del diseño centralizados (`internal val`, antes duplicados como `private val` en cada archivo) — `PalFondoTarjeta` (#E9E9F0, tarjetas Encabezado/Detalle), `PalFondoModal` (#F1EEF7, hoja de Agregar línea), `PalBordeInput` (#D5D7E0), `PalErrorCorridaFinalizada` (#C0392B), etc.
- `CampoSelector.kt`: selector genérico estilo `<select>` (Línea de Producción, Lote en proceso) — toca para abrir/cerrar una lista que empuja el contenido de abajo (el mockup la superpone con `position:absolute`; en Compose eso requeriría `Popup` con posicionamiento manual — se prefirió el enfoque más simple y robusto de empujar contenido, sin cambiar el comportamiento funcional).
- `CampoBuscableProducto.kt`: mismo patrón pero con búsqueda en vivo, reutilizando `PalletCapturaViewModel.buscarProductos`/`productosBusqueda` (ya existían para el modal de línea mixta) — ahora también sirve para el producto del encabezado, que antes era una lista estática TOP 1000 sin buscador.

**Bug real corregido durante la reconstrucción** (no estaba en el diseño, se encontró leyendo el código anterior): al editar una línea de un pallet mixto, el campo de producto se abría vacío en vez de precargado con el producto actual — obligaba a rebuscarlo aunque el usuario no quisiera cambiarlo. Además, la Presentación (Caja/Granel) de esa edición dependía de tener el `ProductoTerminado` completo seleccionado, que nunca se precargaba — así que editar una línea Granel de un pallet mixto mostraba el campo de Cajas en vez de Kilogramos. Corregido: al editar, la Presentación se infiere directamente de cuál campo trae valor en `PalletDetalle` (`cajas != null` → Caja, si no → Granel), sin depender de un `ProductoTerminado` completo.

**Segundo bug real corregido, encontrado después de escribir el nuevo diálogo como overlay propio**: `DialogoLineaDetalle` era un `AlertDialog` (Compose lo cierra solo con el gesto de regresar); al convertirlo en una hoja inferior con `Box` propio para calzar el diseño, ese cierre automático se perdió — el gesto de regresar hubiera navegado la pantalla de Captura hacia atrás con el modal todavía "abierto" en el estado. Se agregó `BackHandler(onBack = onDismiss)` dentro del propio diálogo.

**Pendiente**: sincronizar en Android Studio (Gradle sigue bloqueado en este entorno) y probar: precarga de producto en encabezado/línea mixta al editar, búsqueda en vivo en ambos campos de producto, campo Cajas/Kilogramos correcto en alta y edición (Caja y Granel), gesto de regresar cierra el modal de línea sin salir de la pantalla, Bloquear/Eliminar pallet siguen funcionando igual que antes (solo cambiaron de ícono en `TopAppBar` a botón redondo junto al de regreso).

**Pendiente**: sincronizar en Android Studio y probar en dos dispositivos a la vez (o un dispositivo + escritorio) — crear/editar un Pallet en uno y confirmar que aparece/se actualiza en el otro dentro de ~5s sin salir de la pantalla; confirmar que el gesto de swipe-back funciona igual que las flechas en Configuración y en Pallets (Lista↔Captura↔Inicio).

## Iteración: ocultar Pallets Neutro (Diferencia a Favor/Merma) en la app móvil

El usuario pidió quitar de la app los pallets con folio "0-0000001..." (Pallet Neutro: ajuste de Merma/Diferencia a Favor para cuadrar Corridas, capturado desde escritorio con el boton "Diferencia de Corridas"). El SP `sp_Pallet_Obtener` ya regresaba la columna `EsNeutro`, pero nunca se había mapeado en Android — `Pallet.kt` (dominio) ganó el campo `esNeutro: Boolean`, `PalletSqlServerAdapter.aPallet()` ahora lo lee (`getBoolean("EsNeutro")`), y `PalletsListaViewModel` filtra `!it.esNeutro` tanto en la carga inicial como en el refresco periódico silencioso — la app móvil solo lista pallets reales de producción, sin tocar el SP compartido con escritorio (que sigue mostrando/administrando los Pallets Neutro con normalidad).

**Pendiente**: sincronizar en Android Studio y confirmar que los pallets con folio "0-XXXXXXX" ya no aparecen en la Lista, ni en el refresco cada 5s si se crea uno nuevo desde escritorio mientras la app está abierta.

## Iteración: quitar Bloquear del flujo de alta de Pallet

El usuario pidió quitar el botón de Bloquear específicamente en la pantalla donde se agrega un pallet nuevo. `PalletCapturaScreen.kt` ya distinguía "alta" de "edición" via `palletIdInicial` (null = se entró por el FAB "+", no-null = se entró tocando una tarjeta de la Lista), pero el botón Bloquear se mostraba en cuanto `estadoActual.palletId != null` — que también se vuelve cierto justo después de guardar el encabezado de un pallet nuevo (sigue siendo la misma pantalla de alta, solo que ya tiene Id). Se agregó la condición `palletIdInicial != null` al lado de `puedeModificar` para que Bloquear solo aparezca al editar un pallet que ya existía desde antes de abrir la pantalla. Eliminar no se tocó (no se pidió).

## Iteración: quitar Bloquear por completo de la app móvil

Aclaración del usuario tras el cambio anterior: no era solo ocultar Bloquear en el flujo de alta, sino quitarlo por completo de FrontOne.Android (escritorio lo conserva sin cambios — `PalletsForm`/`PalletEditarForm`/`sp_Pallet_Bloquear` siguen igual). Limpieza completa de la vertical, capa por capa, para no dejar código muerto:

- `PalletCapturaScreen.kt`: se quitó el ícono de Bloquear del header, su `AlertDialog` de confirmación y el estado `mostrarConfirmarBloquear`.
- `PalletCapturaViewModel.kt`: se quitó el método `bloquear()` y la dependencia `BloquearPalletUseCase`.
- `domain/usecase/BloquearPalletUseCase.kt`: eliminado (ya sin ningún llamador).
- `di/DataModule.kt`: se quitó el `@Provides` de `BloquearPalletUseCase`.
- `domain/port/PalletPort.kt` / `data/sqlserver/PalletSqlServerAdapter.kt`: se quitó el método `bloquear(id)` del puerto y su implementación contra `Produccion.sp_Pallet_Bloquear` — nada en Android lo llama ya.

Eliminar sigue intacto (no se pidió tocarlo). Un pallet bloqueado desde escritorio sigue mostrando todo de solo lectura en la app (la app respeta `Bloqueado` para deshabilitar Guardar/Agregar/Editar/Eliminar), solo que ya no hay forma de bloquear uno desde el celular.
